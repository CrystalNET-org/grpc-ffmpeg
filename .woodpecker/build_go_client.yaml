steps:
  build-go-client:
    name: build-go-client
    image: golang:latest
    environment:
      PROTOC_VERSION: 33.2
    commands:
      - apt-get update && apt-get install -y unzip
      - curl -LO https://github.com/protocolbuffers/protobuf/releases/download/v$${PROTOC_VERSION}/protoc-$${PROTOC_VERSION}-linux-x86_64.zip
      - unzip protoc-$${PROTOC_VERSION}-linux-x86_64.zip -d /usr/local
      - export PATH=$PATH:/usr/local/bin
      # Install Go protobuf plugins
      - go install google.golang.org/protobuf/cmd/protoc-gen-go@latest
      - go install google.golang.org/grpc/cmd/protoc-gen-go-grpc@latest
      # Generate Go protobuf code from the proto file with explicit plugin paths
      - PROTOC_GO_PLUGIN_PATH=$(go env GOPATH)/bin/protoc-gen-go
      - PROTOC_GRPC_PLUGIN_PATH=$(go env GOPATH)/bin/protoc-gen-go-grpc
      - protoc --plugin=protoc-gen-go=$${PROTOC_GO_PLUGIN_PATH} --go_out=src/client/go --go_opt=paths=source_relative --plugin=protoc-gen-go-grpc=$${PROTOC_GRPC_PLUGIN_PATH} --go-grpc_out=src/client/go --go-grpc_opt=paths=source_relative src/proto/ffmpeg.proto

      # Navigate to the Go client directory
      - cd src/client/go

      # Initialize Go module
      - go mod init grpc-ffmpeg-client

      # Download Go modules and generate go.sum
      - go mod tidy

      # Build the Go client executable
      - go build -o grpc-ffmpeg-client .
when:
  branch: ${CI_REPO_DEFAULT_BRANCH}
  event: push
